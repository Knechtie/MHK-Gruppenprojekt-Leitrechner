sudo raspi-config und unter localisation de_DE@euro wählen um money typ in postgresql mit "alter DATABASE "CustomerDB" SET lc_monetary = "de_DE@euro"" auf euro zu stellen !!!!


sudo apt-get update
sudo apt-get upgrade
//node Version Manager intallieren:
curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.11/install.sh | bash
--> über nvm npm und node.js installieren
nvm install node
 nvm install-latest-npm
///Mit package.json >npm install<: package.json in root-Verzeichniseinfügen und npm install in dem Verteichnis ausführen, dann Ordner "website" erstellen und dort package.json einfügen und npm install ausführen 

//ohne package.json files:
npm init
npm install async
npm install node-opcua
npm install express
node-red?????????

mkdir website
cd website
npm init
npm install jquery
npm install popper.js
npm install bootstrap

//Tools:
npm install nodemon -g
npm install jshint -g
npm install pm2@latest -g

    //pm2: 
    //Erstellen von ecosystem.config.js im App verzeichnis in dem die .js files zum gelichzeitigen start mit pm2 konfiguriert werden müssen
    pm2 ecosystem
    //Startupskript erstellen und danach den angezeigten befehl ausführen:
    pm2 startup
    //Prozesse beobachten:
    pm2 monit
    //alle prozesse stoppen, für einzelnen die prozessnummer angeben statt all
    pm2 stopp all
    //Prozess aus der liste entfernen, wird gebraucht wenn die startumgebung angepasst werden soll. 
    pm2 delete Prozessnummer


phppgadmin:

https://medium.com/@aggarwaldeepak/how-to-install-postgresql-and-phppgadmin-e055dbb1e3d2

Logger:
npm install winston
Übersicht:
https://github.com/mthenw/frontail
npm i frontail

Filebrowser:
https://www.npmjs.com/package/cloudcmd

Datebank Server:
sudo apt-get install postgresql
Passwort ändern:
https://wiki.ubuntuusers.de/PostgreSQL/#Admin-Passwort-einrichten

QR-Code
https://www.npmjs.com/package/qr-image

//NTP-client
sudo apt-get install ntp
//Automatische aktualisierung der Ziet wenn Internet vorhanden 
sudo apt-get install ntpdate



http://192.168.4.24:3000/
http://192.168.4.24:9001/
http://192.168.4.24:8000/
http://192.168.4.24:1880/

///////ALT:
(https://docs.npmjs.com/getting-started/fixing-npm-permissions
    -> https://docs.npmjs.com/getting-started/installing-node
        -> "Using a version manager to install Node.js and npm")
curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.11/install.sh | bash
(https://github.com/creationix/nvm/blob/master/README.md#installation)

running the demo client from source

 $ git clone https://github.com/node-opcua/node-opcua.git
 $ cd node-opcua
 $ npm install
 $ node packages/node-opcua-samples/bin/simple_client.js -e "opc.tcp://opcserver.mAutomation.net:4841" -n="ns=1;s=EVR2.system.RTC_SEC"


sudo apt-get install nodejs
sudo apt-get install npm
sudo apt-get install git
npm install node-opcua --save
npm install async --save

npm install chalk
npm install better-assert
npm install node-opcua-binary-stream

sudo npm install nodemon -g
npm install npmvet -g

###Für Webinterface


###im Arbeitsverzeichnis:
npm install express



###Visuaal Studio Code:
jshint



Touch-Bildschirm aus:
sudo bash -c "echo 1 > /sys/class/backlight/rpi_backlight/bl_power"
ein:
sudo bash -c "echo 0 > /sys/class/backlight/rpi_backlight/bl_power"
Helligkeit:
sudo bash -c "echo 255 > /sys/class/backlight/rpi_backlight/brightness"


//USB-Stick
muss name mhk haben!!
